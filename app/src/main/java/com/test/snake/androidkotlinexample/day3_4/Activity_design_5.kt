package com.test.snake.androidkotlinexample.day3_4

import android.support.v7.app.AppCompatActivity
import android.os.Bundle
import com.test.snake.androidkotlinexample.R

class Activity_design_5 : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_design_5)
    }

    // 힌트: ScrollView, HorizontalScrollView 사용법
    // 미션 1:
    // 가로로 이동하는 것을 세로로 만들어본다.
}
